module.exports = function calculadora(a,b,operador){
    if(operador == '+'){
        return a + b
    }
    else if(operador = '*'){
        return a * b;
        
    }
    else if(operador = '/'){
        return a / b;
        
    }
    else if(operador = '-'){
        return a - b;   
    }
}